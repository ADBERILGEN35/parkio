/**
 * Anonymous public Explore request bounds — mirrors certified web/public contract.
 * Server also clamps; client must never request above these.
 */
export const PUBLIC_EXPLORE_LIMIT = 6;
export const PUBLIC_EXPLORE_RADIUS_METERS = 5_000;
