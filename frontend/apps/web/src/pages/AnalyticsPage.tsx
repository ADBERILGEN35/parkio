import { PlaceholderPage } from './PlaceholderPage';

export function AnalyticsPage() {
  return (
    <PlaceholderPage
      title="Analytics"
      description="Staff analytics dashboard placeholder. Requires MODERATOR or ADMIN role (gateway + RoleRoute guard)."
    />
  );
}
