import { PlaceholderPage } from './PlaceholderPage';

export function ModerationPage() {
  return (
    <PlaceholderPage
      title="Moderation"
      description="Moderator case management placeholder. Requires MODERATOR or ADMIN role (gateway + RoleRoute guard)."
    />
  );
}
