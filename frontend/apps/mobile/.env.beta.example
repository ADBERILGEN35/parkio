# Hosted beta environment.
# Copy to `.env.beta` (gitignored) for local beta builds, or rely on the EAS
# `preview` profile (eas.json) which injects these at build time.
EXPO_PUBLIC_APP_ENV=hosted-beta
EXPO_PUBLIC_API_BASE_URL=https://api.parkio.dev/api/v1
EXPO_PUBLIC_SMART_RETURN_ENABLED=true
