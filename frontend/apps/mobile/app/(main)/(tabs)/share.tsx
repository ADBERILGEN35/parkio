import { View } from 'react-native';

/**
 * Share tab is intentional chrome only — tabPress opens the upload modal.
 * This screen is not normally shown because tabPress is prevented.
 */
export default function ShareTab() {
  return <View />;
}