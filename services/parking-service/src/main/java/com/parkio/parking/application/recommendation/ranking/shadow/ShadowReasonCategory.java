package com.parkio.parking.application.recommendation.ranking.shadow;

/** Coarse reason categories attributed by the shadow challenger. */
public enum ShadowReasonCategory {
    DISTANCE,
    FRESHNESS,
    CAPACITY,
    CONFIDENCE,
    FAVOURITE
}
