package com.parkio.parking.decision.calibration;

/** Bounded hard-constraint family for shadow analytics. */
public enum HardConstraintFamily {
    NONE,
    INTEGRITY,
    LOCATION,
    LEGALITY,
    OTHER
}
