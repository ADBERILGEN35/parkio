package com.parkio.parking.domain;

/** Vehicle sizes a spot can accommodate. {@code ANY} means no size restriction. */
public enum VehicleType {
    SEDAN,
    HATCHBACK,
    SUV,
    VAN,
    MOTORCYCLE,
    ANY
}
