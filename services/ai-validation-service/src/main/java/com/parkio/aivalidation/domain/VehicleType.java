package com.parkio.aivalidation.domain;

/** Vehicle sizes a detected space is estimated to fit. {@code ANY} is a catch-all. */
public enum VehicleType {
    SEDAN,
    HATCHBACK,
    SUV,
    VAN,
    MOTORCYCLE,
    ANY
}
