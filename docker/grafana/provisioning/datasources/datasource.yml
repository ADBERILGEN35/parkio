apiVersion: 1

# Auto-provisioned Prometheus datasource (reachable over the observability network).
datasources:
  - name: Prometheus
    # Stable uid so provisioned dashboards can reference it portably.
    uid: parkio-prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
